import React from "react";
import { Button } from "@/components/ui/button";

const page = () => {
  return (
    <div>
      page
      <Button>Sign In</Button>
    </div>
  );
};

export default page;
